# mise configuration
#
# .env holds every setting, including secrets, and is not committed.
# Create it with: ./scripts/setup.sh

[env]
_.file = ".env"

[tools]
# uv installs the interpreter that pyproject.toml's requires-python asks for,
# so this only needs to put uv itself on PATH.
uv = "latest"
