# 보안 정책

이 프로젝트는 코레일 KTX 취소표를 감시해 알려주는 개인용 텔레그램 봇입니다.
사용자의 **코레일 로그인 정보(휴대전화번호 + 비밀번호)** 를 직접 다루기 때문에,
일반적인 토이 프로젝트보다 취급 주의가 필요합니다. 이 문서는 취약점을 어디로
알려야 하는지와, 이 봇을 직접 운영할 때 알아둬야 할 보안 사항을 정리합니다.

## 취약점 신고

**공개 이슈로 올리지 마십시오.** 자격증명을 다루는 코드의 취약점은 공개되기 전에
고칠 시간이 필요합니다. 일반 버그는 Issues 로 받지만, 보안 문제는 아래 경로를
써주십시오.

권장 경로는 **GitHub Security Advisory** 입니다. 이 저장소는 Private vulnerability
reporting 이 켜져 있어 바로 사용할 수 있습니다.

1. 저장소의 **Security** 탭으로 이동합니다.
   <https://github.com/thsvkd/korail_KTX_macro_telegrambot/security>
2. **Report a vulnerability** 를 눌러 비공개 신고를 작성합니다.

이 경로는 신고 내용이 저장소 관리자에게만 보이고, 수정이 끝난 뒤에 관리자가
공개 여부를 결정할 수 있습니다.

그래도 신고할 방법이 마땅치 않다면, 저장소 소유자([@thsvkd](https://github.com/thsvkd))
에게 GitHub 프로필에 공개된 경로로 연락해 주십시오. 이때도 취약점의 세부 내용은
공개된 곳에 적지 말고, 연락이 닿은 뒤 비공개 경로로 주고받아 주십시오.

신고에 포함하면 좋은 것:

- 어떤 조건에서 재현되는지 (실행 방식, 설정값, 대화 단계)
- 영향 범위 (자격증명 노출인지, 타인 예약 조작인지, 서비스 중단인지)
- 가능하다면 재현 절차 또는 PoC

신고에 **절대 포함하지 말 것**: 실제 봇 토큰, 코레일 계정 비밀번호, `.env` 파일
전체, 마스킹되지 않은 실제 휴대전화번호. 값 자체가 필요하다면 자리표시자로
바꿔서 보내주십시오.

응답 시간은 개인이 여가 시간에 관리하는 프로젝트라는 점을 감안해 주십시오.
확약된 SLA 는 없습니다.

## 지원 버전

개인 프로젝트라 유지보수 대상은 **`master` 브랜치의 최신 커밋 하나**입니다.
과거 태그나 릴리스에 대한 백포트는 하지 않습니다. 보안 수정은 `master` 에만
반영되므로, 운영 중이라면 최신 `master` 를 받아 재배포하는 것이 유일한
업데이트 경로입니다.

| 대상 | 지원 |
| --- | --- |
| `master` 최신 커밋 | 지원 |
| 그 이전 커밋 / 과거 이미지 태그 | 미지원 |

## 이 프로젝트 특유의 보안 사항

아래는 코드에서 확인한 현재 동작입니다. 직접 운영한다면 반드시 읽어보십시오.

### 코레일 자격증명은 용도별 수명으로 암호화해 저장합니다

- 사용자가 대화 중 입력한 코레일 비밀번호는 Redis 에 **평문으로 저장되지
  않습니다**. `src/korail_bot/utils/crypto.py` 의 `SecretBox` 가 Fernet 으로
  암호화한 값(`v1:` 접두사)만 저장됩니다. 키는 `SESSION_SECRET` 에서
  PBKDF2-HMAC-SHA256(200,000회)으로 유도합니다.
- 일반 세션의 비밀번호는 검색 프로세스에 넘긴 직후 빈 문자열로 지웁니다
  (`handlers/conversation_handler.py`). 세션을 초기화할 때도 같습니다
  (`models/user.py`의 `reset()`).
- 한 번 등록한 계정은 `user_credentials:{chat_id}`에 별도로 암호화해
  `CREDENTIAL_TTL_SECONDS`(기본 90일) 동안 보관합니다. 이 레코드는 예약과 예약
  사이에도 남는 편의 기능이므로 `/logout`, 봇 차단·대화방 삭제 또는 TTL 만료
  전에는 검색이 끝나도 삭제되지 않습니다.
- 재시작 복구(`RESUME_ON_RESTART=true`)를 켜면, 검색이 살아 있는 동안에만
  `resume_credentials:{chat_id}` 라는 **전용 키**에 암호화된 자격증명이
  보관됩니다. 검색이 끝나거나 취소되면 즉시 삭제되고(`delete_resume_credentials`),
  삭제가 누락되는 경우에 대비해 `RESUME_TTL_SECONDS`(기본 3일) TTL 이 걸립니다.
- 복호화에 실패하면(키가 바뀌었거나 값이 변조된 경우) 평문으로 되돌리지 않고
  "자격증명 없음"으로 취급해 사용자에게 다시 입력받습니다.

### 자격증명은 argv 가 아니라 stdin 으로 자식 프로세스에 전달됩니다

검색은 별도의 자식 프로세스(`telegramBot/telebotBackProcess.py`)에서 돕니다.
명령줄 인자는 같은 호스트의 모든 프로세스가 `ps` 나 `/proc` 으로 읽을 수 있기
때문에, 자격증명은 argv 에 넣지 않고 **stdin 첫 줄에 JSON 한 줄**로 넘긴 뒤
파이프를 닫습니다(`services/reservation_service.py`). argv 에는 날짜, 역, 열차
번호처럼 공개 시간표 정보만 들어갑니다.

### 내부 콜백은 루프백 + 토큰 두 가지를 모두 요구합니다

자식 프로세스는 검색 결과를 앱의 HTTP 엔드포인트로 돌려보냅니다. 이 엔드포인트가
열려 있으면 누구든 봇을 통해 임의의 메시지를 보낼 수 있으므로,
`api/auth.py` 의 `verify_internal_request()` 가 두 조건을 함께 검사합니다.

1. 요청 출발지가 루프백(`127.0.0.1`, `::1`, `localhost`)일 것
2. `INTERNAL_CALLBACK_TOKEN` 이 일치할 것 (`hmac.compare_digest` 로 비교)

토큰은 지정하지 않으면 앱이 시작할 때마다 `secrets.token_urlsafe(32)` 로 새로
만들어 환경변수에 넣고, 자식 프로세스가 이를 상속합니다. 토큰이 새어도 루프백이
아니면 통과하지 못하고, 출발지를 위조해도 토큰 없이는 통과하지 못합니다.

### Telegram 업데이트는 long polling으로만 받습니다

앱이 Telegram으로 아웃바운드 연결을 열어 업데이트를 가져오므로, 봇을 쓰는 데
공개 HTTP 엔드포인트가 필요하지 않습니다. 내부 Flask 서버는 기본적으로
`127.0.0.1`에만 바인딩되고 Docker Compose도 그 포트를 호스트에 공개하지 않습니다.

### Mini App API는 켠 만큼만 공개됩니다

`MINI_APP_API_ENABLED=true` 로 미니 앱을 쓰면 이 봇은 처음으로 인터넷에서 닿는
표면을 갖게 됩니다. 그 표면을 좁게 유지하는 것이 아래 설계의 목적입니다.

**리스너를 둘로 나눕니다.** 내부 리스너(`FLASK_PORT`)에는 검색 프로세스가
결과를 보고하는 `/reservation-callback`과 `/check_payment`가 있고, 공개
리스너(`MINI_APP_API_PORT`)에는 미니 앱 API와 페이지만 있습니다.

이 분리는 편의가 아니라 필수입니다. `/reservation-callback`은 임의의 채팅방에
임의의 메시지를 보낼 수 있고, 자신을 지키는 수단은 "요청이 루프백에서 왔는가"와
프로세스 토큰입니다. 그런데 **출발지 검사는 앞에 무엇을 두느냐에 따라 의미를
잃습니다.**

호스트에서 `tailscale serve`로 프록시하면 프록시가 로컬에서 앱에 연결하므로
바깥에서 온 요청도 `remote_addr`이 `127.0.0.1`로 도착합니다. 실측으로 확인한
값입니다 — 일회용 에코 서버를 붙여 보니 프록시를 통과한 요청이
`remote_addr=127.0.0.1`, `X-Forwarded-For=<클라이언트 주소>`로 들어왔습니다.
그 구성이었다면 남는 방어선은 토큰 하나뿐이었을 것입니다.

지금 쓰는 사이드카 구성에서는 프록시가 컨테이너 네트워크에서 오므로
`remote_addr`이 루프백이 아니고, 그래서 이 특정 경로로는 뚫리지 않습니다. 하지만
그것은 **앞단 구성이 우연히 그렇다는 뜻일 뿐**이고, 앞단은 언제든 바뀝니다.
그래서 검사를 믿는 대신 그 경로를 노출되는 소켓에 아예 올리지 않습니다. 성질이
구성이 아니라 구조에 있으므로, 나중에 누가 라우트를 추가하며 어느 앱에 붙이는지
착각해도 유지됩니다. 공개 주소로 `/reservation-callback`을 호출하면 404입니다.

**모든 요청은 Telegram의 서명을 통과해야 합니다.** 채팅 흐름은 발신자를 물을
필요가 없었습니다 — 업데이트는 Telegram 서버에서 오고 거기 실린 chat_id는
사실입니다. 미니 앱은 그 반대입니다. 페이지는 사용자의 폰에서 돌며 이 앱을 직접
호출하므로, 모든 요청은 발신자가 고른 chat_id를 달고 옵니다. 그래서 chat_id는
**오직 `initData`의 HMAC-SHA256 서명에서만** 결정합니다. 키는 봇 토큰에서
파생되므로 토큰 없이는 위조할 수 없고, 요청 본문에 적힌 chat_id는 어디서도
읽지 않습니다. 서명에는 유효 기간(`MINI_APP_INIT_DATA_TTL_SECONDS`, 기본 24시간)이
있어, 기기에서 새어 나간 payload가 영구적인 열쇠가 되지 않습니다.

**호출 빈도에 상한이 있습니다.** 유효한 서명이 초당 열 번 물어볼 권리는 아닙니다.
철도사에 실제로 접속하는 엔드포인트(열차 조회, 로그인, 검색 시작)에는 더 낮은
상한을 따로 둡니다. 무제한 호출은 이 호스트가 아니라 철도사 서버에 비용을
지웁니다.

**응답은 자격증명을 담지 않습니다.** 저장된 철도 비밀번호는 로그인에만 쓰이고
어떤 응답에도 실리지 않습니다. 예약 취소는 요청이 예약번호를 지정하지 않고
해당 채팅의 결제 대기 목록에서 가져옵니다 — 예약번호는 추측 가능하고, 철도사는
남의 예약도 기꺼이 취소해 주기 때문입니다. 예기치 못한 오류의 트레이스백은
로그에만 남고 응답에는 일반 문구만 나갑니다.

**미니 앱은 채팅의 규칙을 우회하지 못합니다.** 접근 승인 검사, 체험 횟수 차감,
중복 검색 방지는 두 경로가 함께 쓰는 `start_booking` 하나에 있습니다. 조건은
채팅과 같은 `InputValidator`로 다시 검증하고, 열차 번호는 검색 프로세스의 인자가
되므로 숫자인지 개수 상한 안인지 확인합니다.

**페이지는 봇이 직접 서빙합니다.** 예전에는 정적 호스팅이 변조되면 거짓 입력
화면을 보여줄 수 있다는 경계가 있었고, 자격증명을 받지 않는다는 점이 피해를
제한했습니다. 이제는 계정 등록도 이 화면에서 하므로 그 완화책이 사라졌고, 대신
페이지와 API가 같은 오리진이라 끼어들 제3자 자체가 없습니다. CORS도 열지 않고,
응답에는 `frame-ancestors`를 Telegram으로 제한한 CSP와 `nosniff`가 붙으며, JSON
응답은 `no-store`입니다.

**남는 위험은 이렇습니다.** 서비스 거부(상한으로 완화하되 제거되지는 않음), API
계층 자체의 결함, 그리고 봇 토큰 유출입니다. 마지막 것은 미니 앱 여부와 무관하게
이미 봇 전체를 내주는 사건이지만, 미니 앱을 켜면 서명 위조까지 가능해집니다.

`MINI_APP_URL`은 HTTPS만 허용합니다. `MINI_APP_API_ENABLED` 없이 URL만 설정하면
열리기는 하되 아무 동작도 하지 않는 화면이 되므로 시작 로그가 경고합니다.

### `SESSION_SECRET` 의 역할

`SESSION_SECRET` 은 위의 자격증명 암호화 키를 만드는 **키 재료**입니다.
`scripts/setup.sh secrets` 로 생성할 수 있습니다.

- 설정하지 않으면 프로세스마다 임시 키가 생성되고, 앱을 재시작하는 순간
  저장된 자격증명을 읽을 수 없게 됩니다. 시작 시 경고가 출력됩니다.
- 따라서 `RESUME_ON_RESTART=true` 인데 `SESSION_SECRET` 이 없으면, 중단된 검색은
  **절대 재개되지 않습니다**. 재개에 필요한 자격증명이 바로 그 재시작 때문에
  읽을 수 없게 되기 때문입니다.
- 값을 교체하면 기존에 저장된 자격증명은 모두 읽을 수 없게 됩니다. 사용자는
  다시 입력하면 되지만, 진행 중이던 검색은 복구되지 않습니다. 체험 횟수와 승인
  레코드의 전화번호 해시도 이 값으로 HMAC하므로 기존 승인 역시 다시 받아야 합니다.
- 이 값은 시크릿입니다. `.env` 에만 두고 저장소나 이슈, CI 로그에 노출하지
  마십시오.

### `USERID` / `USERPW` 는 개발자 방에서만 쓰입니다

`USERID` 와 `USERPW` 는 개발·테스트를 위해 고정 코레일 계정을 지정하는
옵션입니다. **개발자 모드인 채팅방에서만** 적용되어, 그 방에서는 휴대전화번호와
비밀번호를 묻지 않고 이 계정으로 로그인합니다. 일반 사용자는 이 값이 설정돼
있든 아니든 자기 계정을 등록해서 씁니다.

과거에는 설정만 하면 **누가 말을 걸든** 이 계정으로 예매했습니다. 봇은 이름으로
검색되고 링크만 알면 누구나 대화할 수 있으므로, 그것은 운영자의 코레일 계정을
사실상 공개하는 것과 같았습니다. 지금은 개발자 방으로 한정됩니다.

따라서 이 값을 채워둘 때 지켜야 할 것은 하나입니다 — **`ADMIN_MAGIC_STRING`
을 충분히 길고 추측 불가능하게 두십시오.** 그 문자열을 아는 사람은 자기
채팅방을 개발자 방으로 만들 수 있고, 그러면 이 계정에 닿습니다. 16자 미만이면
앱이 기동할 때 경고합니다. `ADMIN_MAGIC_STRING` 을 설정하지 않으면 개발자 모드
자체가 비활성이므로 `USERID`/`USERPW` 는 아무 데서도 쓰이지 않습니다(앱이 그
사실도 기동 시 알려줍니다).

### 그 밖에 운영 시 확인할 것

- `ADMIN_PASSWORD` 를 설정하지 않으면 모든 관리자 명령이 거부됩니다. 설정한
  경우 실패 횟수가 `ADMIN_MAX_AUTH_FAILURES`(기본 5) 를 넘으면
  `ADMIN_LOCKOUT_SECONDS`(기본 900초) 동안 잠깁니다. 이 값은 코레일 비밀번호와
  **다른 값**을 쓰십시오 — 의도적으로 분리되어 있습니다.
- `REDIS_PASSWORD` 를 비워두면 경고가 나옵니다. Redis 가 컨테이너 네트워크
  밖에서 접근 가능하지 않은지 반드시 확인하십시오. 자격증명이 암호화되어
  있더라도 예약 정보와 chat_id 는 그대로 들어 있습니다.
- `FLASK_DEBUG=true` 는 Werkzeug 디버거를 통한 원격 코드 실행을 허용합니다.
  외부에서 접근 가능한 호스트에서는 절대 켜지 마십시오.
- 배포 전 `./scripts/setup.sh check` 를 실행하면 위 항목들(미설정 시크릿,
  플레이스홀더 값, 디버그 서버, 노출된 Redis, 커밋된 자격증명)을 한 번에
  점검합니다.

### 알려진 한계

정직하게 적어둡니다.

- **chat_id 는 마스킹되지 않습니다.** 코레일 ID(휴대전화번호)는
  `utils/privacy.py` 의 `mask_phone()` 으로 `010-****-5678` 형태로 마스킹되지만,
  텔레그램 chat_id 는 로그에 원문으로 남습니다. chat_id 는 개인 식별자이므로,
  로그를 공개된 곳(예: CI 실행 로그)으로 내보내지 마십시오.
- **자격증명이 프로세스 메모리에는 존재합니다.** 저장소 수준의 보호일 뿐,
  호스트를 장악당한 경우까지 막지는 못합니다.
- 이 봇은 코레일과 무관한 비공식 개인 프로젝트입니다. 자동화 조회가 코레일
  이용약관에 저촉될 수 있으며, 사용에 따른 계정 제재 등의 책임은 사용자에게
  있습니다. 반드시 **본인 명의 계정으로만** 사용하십시오.

## 범위 밖

다음은 취약점으로 접수하지 않습니다.

- 사용자가 `.env` 를 잘못 설정해 생긴 문제 (예: `FLASK_DEBUG=true` 로 노출,
  Redis 를 인터넷에 개방)
- 코레일 사이트 변경으로 인한 동작 실패 — 버그로 신고해 주십시오
- 봇 토큰이나 코레일 계정을 사용자가 직접 유출한 경우
- 의존성의 취약점 자체 — Dependabot 이 주간으로 갱신 PR 을 올립니다. 다만 이
  프로젝트에서 실제로 악용 가능한 경로가 있다면 신고해 주십시오.
